import LayoutContent from '../(home)/components/layoutContent/LayoutContent';

const ModelsLayout = ({ children }: { children: React.ReactNode }) => {
  return <LayoutContent>{children}</LayoutContent>;
};

export default ModelsLayout;
